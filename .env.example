# Example environment variables for local development and production

# Backend MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/paper_plane_db

# Backend port (optional, defaults to 5001)
PORT=5001

# Frontend URL for CORS in backend (set to your deployed frontend URL in production)
RENDER_FRONTEND_URL=http://localhost:5173

# Frontend backend URL for API calls (used by frontend)
VITE_BACKEND_URL=http://localhost:5001
